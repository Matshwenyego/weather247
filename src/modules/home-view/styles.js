import {StyleSheet} from 'react-native';
import {colors} from 'assets';

const {
  white,
  sunny
} = colors;

const styles = StyleSheet.create({
    container: {
      flex: 1,
    },
});

export {styles};